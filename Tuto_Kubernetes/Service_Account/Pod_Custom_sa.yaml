apiVersion: v1
kind: Pod
metadata:
  name: sa-test
  namespace: sa-demo
spec:
  serviceAccountName: demo-sa
  containers:
  - name: kubectl-container
    image: bitnami/kubectl
    command: ["sleep", "3600"]
